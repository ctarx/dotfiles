#!/usr/bin/env bash

set -euo pipefail

if ! usage=$(du -sh -- "$HOME"); then
  printf 'Error: could not calculate disk usage for %s\n' "$HOME" >&2
  exit 1
fi

usage=${usage%%[[:space:]]*}
printf '\033[33mused %siB\033[0m\n' "$usage"
